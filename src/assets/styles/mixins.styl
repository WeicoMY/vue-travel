
rem(size)
  (size/$htmlSize)rem

rem2(size)
  (size/$htmlSize/2)rem

ellipsis()
  overflow hidden
  text-overflow ellipsis
  white-space: nowrap

backIcon(size=rem2(32), weight=700)
  .icon-back::before
    content "\e624"
    font-size size
    font-weight weight